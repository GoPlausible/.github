# Copy to .env (defaults are fine for the local flow).
# The paid endpoint. Default /my-api is proxied by Vite to http://localhost:4021
# (see vite.config.ts) so no CORS setup is needed on the server.
VITE_RESOURCE_SERVER_URL=/my-api
